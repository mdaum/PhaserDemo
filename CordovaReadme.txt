Cordova-Android Setup:
1. Get Android SDK (normally w/ android studios)
2. clone cordova-android repo https://github.com/apache/cordova-android
3. For Cordova command-line tools to work, 
or the CLI that is based upon them, you need to include the SDK's tools and platform-tools
directories in your PATH. On a Mac, you can use a text editor to create or modify
the ~/.bash_profile file, adding a line such as the following, depending on where the SDK installs: